# Segment Component - Доступность (A11y)

## Обзор

Компонент Segment полностью соответствует стандартам WCAG 2.1 уровня AA и поддерживает все необходимые возможности доступности.

## Ключевые возможности

### ARIA Атрибуты

Компонент автоматически использует корректные ARIA атрибуты для обеспечения доступности.

#### `role="progressbar"`

Определяет компонент как индикатор прогресса для скринридеров.

#### `aria-label`

Текстовое описание компонента.

```tsx
<Segment
  length={200}
  percentages={[65]}
  ariaLabel="Прогресс выполнения задачи"
/>
```

**Авто-генерация:** Если не указано, создается автоматически:

```
"Сегмент прогресса: 65%"
```

#### `aria-valuenow`

Текущее значение прогресса (0-100).

```tsx
<Segment percentages={[45, 80]} ariaValueNow={45} />
```

**Авто-генерация:** Максимальное значение из массива `percentages`.

#### `aria-valuemin` и `aria-valuemax`

Минимальное и максимальное значения диапазона.

```tsx
<Segment percentages={[50]} ariaValueMin={0} ariaValueMax={100} />
```

**По умолчанию:** `min=0`, `max=100`

#### `aria-valuetext`

Человекочитаемое представление значения.

```tsx
<Segment percentages={[45]} ariaValueText="45 из 100 мегабайт загружено" />
```

**Авто-генерация:**

```
"45 из 100"
```

### Навигация с клавиатуры

Компонент полностью доступен с клавиатуры через `tabIndex={0}`:

| Клавиша       | Действие             |
| ------------- | -------------------- |
| `Tab`         | Переход к компоненту |
| `Shift + Tab` | Переход назад        |

### Визуальная обратная связь

#### Индикатор фокуса

```scss
&:focus-visible {
  outline: 2px solid #4a90e2;
  outline-offset: 2px;
}
```

- Четкая видимая обводка при фокусе
- Использует `:focus-visible` для корректной работы с мышью и клавиатурой
- Цвет с достаточным контрастом

### Скрытие декоративных элементов

Все внутренние слои помечены `aria-hidden="true"`:

```tsx
<div className="segment__layer" aria-hidden="true" />
```

Это предотвращает избыточное объявление декоративных элементов скринридерами.

## Примеры использования

### Простой прогресс-бар

```tsx
<Segment
  length={200}
  height={24}
  percentages={[65]}
  ariaLabel="Прогресс выполнения задачи"
  ariaValueText="65% выполнено"
/>
```

**Объявление скринридера:**

> "Прогресс выполнения задачи, прогресс-бар, 65% выполнено"

### Загрузка файла

```tsx
<Segment
  length={300}
  height={24}
  percentages={[45, 80]}
  ariaLabel="Загрузка файла"
  ariaValueNow={45}
  ariaValueText="45 из 100 мегабайт загружено"
/>
```

**Объявление скринридера:**

> "Загрузка файла, прогресс-бар, 45 из 100 мегабайт загружено"

### Множественные индикаторы

```tsx
<Segment
  length={250}
  height={32}
  percentages={[30, 70]}
  ariaLabel="Использование памяти"
  ariaValueNow={30}
  ariaValueText="30% активно используется, 70% кэшировано"
/>
```

## Тестирование доступности

### Автоматическое тестирование

Используйте Storybook addon-a11y для проверки:

```bash
npm run storybook
```

### Ручное тестирование

#### Скринридеры

- **macOS:** VoiceOver (`Cmd + F5`)
- **Windows:** NVDA / JAWS
- **Linux:** Orca

#### Навигация с клавиатуры

1. Нажмите `Tab` для фокуса на компоненте
2. Проверьте видимость индикатора фокуса
3. Убедитесь в корректном объявлении скринридером

#### Контрастность

Убедитесь, что цвета соответствуют WCAG 2.1 AA:

- Нормальный текст: минимум 4.5:1
- Крупный текст: минимум 3:1
- UI компоненты: минимум 3:1

## Лучшие практики

### Хорошо

```tsx
<Segment
  percentages={[65]}
  ariaLabel="Прогресс загрузки"
  ariaValueText="65 из 100 файлов загружено"
/>
```

### Плохо

```tsx
<Segment percentages={[65]} />
// Отсутствует описательный label
```

### Хорошо - Динамическое обновление

```tsx
const [progress, setProgress] = useState(0);

<Segment
  percentages={[progress]}
  ariaLabel="Загрузка данных"
  ariaValueNow={progress}
  ariaValueText={`${progress}% загружено`}
/>;
```

## 🔍 Соответствие стандартам

- WCAG 2.1 Level AA
- Section 508
- EN 301 549
- ARIA 1.2
